#include <stdio.h>

/**
 * main - prints the name of the program to the std out
 * @argc: the number of argument passed
 * @argv: array of pointers to the arguments
 * Return: Always 0
 */

int main(unsigned int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
